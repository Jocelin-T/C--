# What can be done to optimize the code

1. `std::string_view` instead of `std::string`
   - Only when reading a String

2. `std::span` to explore a data structure (Container)
   - Only when reading a structure (Container)
